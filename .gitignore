# Secrets — never commit
.env
.env.local
.streamlit/secrets.toml

# Your documents and generated output
Accounts/
documents/*
!documents/.gitkeep
.cache/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
venv/
.venv/

# Test-only Node dependency (jsdom). The app never loads it, and the suite
# skips the tests that need it when it is not installed.
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# Claude Code / AI assistant working files
.claude/
CLAUDE.md
.mcp.json
